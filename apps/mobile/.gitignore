# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native build output (managed workflow — regenerated by prebuild)
android/
ios/

# Signing material — never in git (docs/00 rule 11)
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision
