/**
 * Shared kernel: value objects (Money, LocalizedText), API envelope types (ApiError, Page) and
 * common exceptions (docs/09). Must not depend on any domain module.
 */
package com.tishreen.api.shared;
