/**
 * Platform module: cross-cutting infrastructure — notification, storage, payment, chatbot providers,
 * audit, settings, logging and backup (docs/09). The only package allowed to import provider SDKs.
 */
package com.tishreen.api.platform;
